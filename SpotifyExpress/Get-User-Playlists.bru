meta {
  name: Get-User-Playlists
  type: http
  seq: 5
}

get {
  url: https://api.spotify.com/v1/users/{{user_id}}/playlists?limit=1
  body: none
  auth: bearer
}

params:query {
  limit: 1
}

auth:bearer {
  token: {{token}}
}
